//Calcular el exponente de un numero
let num1=7, exp=2, resultado=1, i=0
        console.warn(`El numero es: ${num1} El exponente es: ${exp}`)
        for(i;i<exp;i++){
            resultado=resultado*num1
 }
 console.log(resultado)
